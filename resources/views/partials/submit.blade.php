<div class="row">
    <!-- Submit Field -->
    <div class="form-group col-sm-12 pull-right">
        {!! Form::submit('Salvar', ['class' => 'btn btn-primary']) !!}
        <a href="{{ $cancelLink }}" class="btn btn-default">Cancelar</a>
    </div>
</div>
